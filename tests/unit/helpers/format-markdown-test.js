import {
  formatMarkdown
} from 'embercliapp/helpers/format-markdown';

module('FormatMarkdownHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatMarkdown(42);
  ok(result);
});
